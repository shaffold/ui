# --- Private registry (optional) ---
# Set a token to require authentication for the registry. The registry is then
# served via app/r/[...name]/route.ts, which checks this token.
# Build the private registry with: pnpm registry:build:private
# REGISTRY_TOKEN=your_secret_token_here
